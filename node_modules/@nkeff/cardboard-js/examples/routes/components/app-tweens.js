import { makeTween, tween, } from '../../../dist/ext/tween.js';
const opacityEnd = { opacity: 1 };
const opacityStart = { opacity: 0 };
export const fadeIn = (tag) => makeTween({
    from: Object.assign({}, opacityEnd),
    to: Object.assign({}, opacityStart),
    duration: 350,
    easing: tween.Easing.Quadratic.InOut,
    update: (value) => tag.setStyle({ opacity: `${value.opacity}` }),
});
export const fadeOut = (tag) => makeTween({
    from: Object.assign({}, opacityStart),
    to: Object.assign({}, opacityEnd),
    duration: 350,
    easing: tween.Easing.Quadratic.InOut,
    update: (value) => tag.setStyle({ opacity: `${value.opacity}` }),
});
//# sourceMappingURL=app-tweens.js.map